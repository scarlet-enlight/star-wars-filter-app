<UserControl x:Class="StarWarsFilterApp.View.FilterFilmView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:StarWarsFilterApp.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
        <ColumnDefinition Width="3*"/>
        <ColumnDefinition Width="4*"/>
    </Grid.ColumnDefinitions>
    <StackPanel Grid.Column="0">
        <Label Content="Title:"/>
        <TextBox x:Name="tbx_title"/>

        <Label Content="Music:"/>
        <TextBox x:Name="tbx_music"/>

        <Label Content="Battles:"/>
        <TextBox x:Name="tbx_battles"/>

            <Button x:Name="btn_filter" Content="Filter" 
                Background="DarkBlue" Foreground="White"
                Margin="20 5" Command="{Binding FilterFilmsCommand}"/>
            <Button x:Name="btn_startView" Content="Back" 
                Background="DarkGray" Foreground="White"
                Margin="20 5" Command="{Binding ReturnCommand}"/>
        </StackPanel>
        <ListBox Grid.Column="1" x:Name="lb_films"
            SelectionChanged="Lb_films_SelectionChanged"/>
    </Grid>
</UserControl>
